# 악어슬라이서 v1.0.2 필수 패키지
# 이미지 처리
Pillow>=10.0.0

# PSD 파일 지원
psd-tools>=1.9.0

# 네트워크 요청 (자동 업데이트)
requests>=2.31.0

# 빌드 도구
pyinstaller>=5.13.0

# 선택적 패키지 (성능 향상)
# numpy>=1.24.0  # 이미지 처리 가속 (선택사항)
# opencv-python>=4.8.0  # 고급 이미지 처리 (선택사항) 